import styled from 'styled-components';

const ButtonStyled = styled.button`
  padding: 1rem;
  cursor: pointer;
  background: linear-gradient(hsla(94, 35%, 60%, 1), hsla(94, 35%, 55%, 1));
  border: none;
  border: 2px solid hsla(295, 40%, 40%, 1);
  border-radius: 3px;
  color: hsla(295, 40%, 30%, 1);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 1.5px;
`;

export default ButtonStyled;
